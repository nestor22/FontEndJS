//clase de for 


var persona ={
	nombre:'nestor',
	apellid:'gonzalez',
	edad:28,
	peso:27

}


console.log(`al inicio del anio ${persona.nombre} pesa ${persona.peso}`)
const INCREMENTO_PESO =200 
const aumentarDePeso = persona=>persona.peso +=INCREMENTO_PESO
const disminuyePeso = persona=>persona.peso -=INCREMENTO_PESO

const META = scha.peso -3

const ComeMucho = () => Math.random() < 0.3
const realizaDeporte() => Math.random() < 0.4

var dias =0

while (persona.peso> META){
		if(ComeMucho()){
				aumentaDePeso(persona)
		}
		if(relizaDeporte(){
				realizaDeporte()
		}
		var dias+=1

}
console.log(` pasaron ${persona.nombre} adelgazo y pesa %{persona.peso.toFixed(2)}`

		 



console.log(`al final del anio ${persona.nombre} pesa ${persona.peso.toFixed(2)}`)
